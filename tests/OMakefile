.PHONY: testlib clean

TESTS[] =
   test
   resolver

OCAMLINCLUDES += .. $(LIBDIR)

#OCAMLFLAGS += -rectypes

foreach(name, $(TESTS))
   OCAML_OTHER_LIBS =
   OCAML_LIBS = ../mlresolv
   OCAML_LINK_FLAGS = -linkall
   OCamlProgram($(name), $(name))
   $(name): $(addsuffix .ml, $(name))

testlib: $(TESTS)

clean:
   $(CLEAN) $(TESTS)
